import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    inputs: {
        border: 'solid',
        borderWidth: 2,
        padding: 10,
        margin: 10,
        borderRadius: 10,
    },
    signUpButton: {
        backgroundColor: '#000',
        padding: 10,
        borderRadius: 10,
        width: '95%',
        margin: 'auto'
    },
    signUpButtonText: {
        color: '#fff',
        textAlign: 'center',
        letterSpacing: 1,
    },
});

export default styles;
